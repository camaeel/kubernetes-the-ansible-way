kind: EncryptionConfig
apiVersion: v1
resources:
  - resources:
      - secrets
    providers:
      - aescbc:
          keys:
            - name: key1
              secret: {{ lookup('password', 'pki/encryption.secret length=64') | b64encode }}
      - identity: {}
